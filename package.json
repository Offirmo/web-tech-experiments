{
  "name": "html_tests",
  "description": "various web techs tests",
  "author": "Offirmo <offirmo.net@gmail.com>",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "b": "bower prune; bower install; bower update",
    "postinstall": "npm install --global nodemon puer strongloop && npm update -g nodemon puer strongloop && npm prune && bower prune && bower install && bower update && cd server && npm prune && npm update",
    "lint": "echo TODO linting"
  },
  "engines": {
    "node": "^0.10.0"
  },
  "dependencies": {
    "bower":               "latest",
    "generator-famous":    "latest",
    "grunt":               "latest",
    "yo":                  "^1"
  },
  "devDependencies": {
    "eslint":              "latest",
    "jshint":              "latest",
    "pre-commit":          "0.0.9"
  },
  "pre-commit": [
    "lint"
  ]
}
