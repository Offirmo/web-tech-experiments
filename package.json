{
	"name": "html_tests",
	"description": "various web techs tests",
	"author": "Offirmo <offirmo.net@gmail.com>",
	"version": "0.0.1",
	"private": true,
	"scripts": {
		"postinstall": "npm install --global nodemon puer; npm update -g nodemon puer; npm prune; npm update; bower prune; bower install; bower update; cd server; npm prune; npm update; cd ../incubator/node_and_common; npm prune; npm update; cd ../base-objects.js; npm prune; npm update"
	},
	"dependencies": {
		"bower":               "latest",
		"generator-famous":    "latest",
		"grunt":               "latest",
		"yo":                  "latest"
	}
}
