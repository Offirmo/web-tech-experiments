{
  "name": "html_tests",
  "description": "various web techs tests",
  "author": "Offirmo <offirmo.net@gmail.com>",
  "version": "0.0.1",
  "private": true,
  "license": "Unlicense",
  "scripts": {
    "puer": "puer",
    "bower": "bower update --config.interactive=false",
    "postinstall": "npm run bower && cd server && npm install",
    "lint": "eslint .",

    "cleardeps": "rm -rf node_modules && rm -rf server/node_modules && rm -rf bower_components",
    "install_globals": "npm install --global puer strongloop && npm update -g puer strongloop",
    "devinstall": "npm run install_globals && npm prune && npm install && npm update && npm run devbower && cd server && npm prune && npm install && npm update",
    "devbower": "bower prune && bower install && bower update",

    "bb": "npm run devbower",
    "cc": "npm run cleardeps",
    "ii": "npm run devinstall"
  },
  "engines": {
    "node": "^0.12"
  },
  "dependencies": {
  },
  "devDependencies": {
    "bower":               "latest",
    "grunt":               "latest",
    "generator-famous":    "latest",
    "pre-commit":          "latest",
    "puer":                "latest",
    "strongloop":          "latest",
    "yo":                  "latest"
  },
  "pre-commit": [
  ]
}
