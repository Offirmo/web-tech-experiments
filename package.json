{
  "name": "html_tests",
  "description": "various web techs tests",
  "author": "Offirmo <offirmo.net@gmail.com>",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "b": "bower prune && bower install && bower update",
    "postinstall": "npm install --global nodemon puer strongloop && npm update -g nodemon puer strongloop && npm prune && npm run b && cd server && npm prune && npm update",
    "cleardeps": "rm -rf node_modules && rm -rf server/node_modules && rm -rf bower_components"
  },
  "engines": {
    "node": "^0.10.0"
  },
  "dependencies": {
    "bower":               "latest",
    "generator-famous":    "latest",
    "grunt":               "latest",
    "yo":                  "latest"
  },
  "devDependencies": {
    "pre-commit":          "latest"
  },
  "pre-commit": [
  ]
}
