{
	"name": "html_tests",
	"description": "various web techs tests",
	"author": "Offirmo <offirmo.net@gmail.com>",
	"version": "0.0.1",
	"private": true,
	"scripts": {
		"i": "npm install --global grunt-cli bower yo generator-famous serve nodemon; npm update -g; bower prune; bower install; bower update; cd server; npm prune; npm update; cd ../incubator/node/assuming_console; npm prune; npm update; cd ../pretty_signals; npm prune; npm update; cd ../../base-objects.js; npm prune; npm update"
	}
}
